{
  "name": "campus",
  "private": true,
  "version": "1.0.0",
  "description": "Campus — school and college management system (NestJS + Prisma + PostgreSQL + React)",
  "scripts": {
    "install:all": "cd apps/api && npm install && cd ../web && npm install",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "setup": "cd apps/api && npx prisma generate && npx prisma db push && npm run seed",
    "dev:api": "cd apps/api && npm run dev",
    "dev:web": "cd apps/web && npm run dev"
  }
}
